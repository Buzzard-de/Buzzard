import AdminLocalizationPanel from "@/components/admin/AdminLocalizationPanel";

export const metadata = {
  title: "Admin Localization – Buzzard",
  robots: { index: false, follow: false },
};

export default function AdminLocalizationPage() {
  return <AdminLocalizationPanel />;
}
