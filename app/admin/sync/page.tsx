import AdminSyncPanel from "@/components/admin/AdminSyncPanel";

export const metadata = {
  title: "Admin Sync – Buzzard",
  robots: { index: false, follow: false },
};

export default function AdminSyncPage() {
  return <AdminSyncPanel />;
}
