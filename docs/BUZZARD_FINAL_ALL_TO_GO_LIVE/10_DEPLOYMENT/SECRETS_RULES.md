SECRETS RULES

Production secrets only via deployment secret manager/environment. Never git, source code, fixtures, SQLite business tables, logs, screenshots, admin UI or AI context. Rotate compromised credentials. Production flags require explicit review. Sales stays closed until final gate.
