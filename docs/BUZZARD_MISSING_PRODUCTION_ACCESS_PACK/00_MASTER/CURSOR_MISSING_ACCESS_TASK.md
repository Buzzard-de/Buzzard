# BUZZARD — MISSING PRODUCTION ACCESS & LIVE ACTIVATION TASK

The Buzzard software #347–#354 is already implemented and tested.
DO NOT rebuild those engines.

This pack prepares the remaining REAL-WORLD ACCESS, DEPLOYMENT and CONTROLLED LIVE VALIDATION work.

OBJECTIVE:
Turn the existing production-ready software from BLOCKED/NOT_CONFIGURED into genuinely validated production capability when the real external accounts and credentials are supplied.

NON-NEGOTIABLE:
- Never invent credentials or API responses.
- Never put secrets in Git, source code, SQLite business tables, logs, screenshots, admin UI or AI context.
- Never treat MOCK/SANDBOX as LIVE.
- Missing credentials = NOT_CONFIGURED.
- Missing API access = NOT_AVAILABLE.
- Missing genuine evidence = UNVERIFIED.
- Blocked gate = BLOCKED.
- Do not enable production automatically.
- Do not open sales automatically.
- Real side effects require the existing controlled approval gates.

Implement/verify all access adapters and deployment diagnostics below, then run only those live checks for which genuine credentials exist.

FINAL OUTPUT must list:
credential status, provider status, live validation status, production flags, genuine side-effect counters and remaining blockers.
