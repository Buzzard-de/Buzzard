# Production gaps / integrations

The repository is consolidated and runnable as a local foundation. The following require real provider choices and credentials before production use: a concrete LLM adapter, a compliant search/web provider, production database, secrets manager, observability stack, backups, deployment infrastructure, and legal/compliance review.

Do not treat placeholder adapters as completed external integrations.
