# Buzzard KI Gesamt

Zentraler Sicherungsordner für alle KI-/Intelligence-Bausteine.

```bash
npm run backup:ki        # Vollsicherung inkl. ZIP
npm run backup:ki:quick  # Index + Symlinks + Manifeste (ohne Snapshot)
```

Siehe `INDEX.md` und `MANIFEST.json` nach dem ersten Build.
