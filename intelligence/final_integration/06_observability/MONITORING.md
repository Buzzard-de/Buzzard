# Monitoring
Monitor connector availability, API errors, rate limits, latency, queues,
data freshness/quality, agent health, memory/database health, resources,
backup status, unusual changes and security events.
Alerts must identify what/when/source/severity/action without exposing secrets.
