{
  "name": "buzzard-api",
  "version": "1.0.0",
  "description": "Buzzard Node API (SQLite, admin modules, plugins)",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "fast-xml-parser": "^5.7.0",
    "jsonwebtoken": "^9.0.2"
  }
}
